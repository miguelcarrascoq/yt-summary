import type { ThemeConfig } from 'antd';

const theme: ThemeConfig = {
    token: {
        "colorPrimary": "#B70283",
    },
    components: {
        Card: {
            headerBg: "#17697a24",
        }
    }
};

export default theme;